---
all:
  hosts:
    devops-server-1:
      ansible_host: 192.168.1.100
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/devops-agent.pem

  vars:
    ansible_python_interpreter: /usr/bin/python3
    environment: production
